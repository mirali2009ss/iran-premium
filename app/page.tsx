import Link from 'next/link'
import {notFound} from 'next/navigation'
import {ArrowLeft,ArrowUpRight} from 'lucide-react'
import {cities} from '../../../lib/data'
export function generateStaticParams(){return cities.map(c=>({slug:c.slug}))}
export default async function CityPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const city=cities.find(c=>c.slug===slug);if(!city)return notFound();return <main className="cityPage"><Link className="back" href="/#cities"><ArrowLeft/> ALL CITIES</Link><div className="cityHero"><img src={city.image} alt={city.name}/><div><small>{city.tag}</small><h1>{city.name}</h1><p>{city.desc}</p></div></div><section className="cityEditorial"><span>THE CITY FILE / {city.fa}</span><h2>A city is more than<br/><i>its monuments.</i></h2><div className="cityColumns"><p>Every city holds a different tempo: a morning market, a shaded courtyard, a late-night street, a view that only makes sense after sunset.</p><p>This page is structured as a living editorial chapter — ready for history, culture, architecture, food, places to visit, gallery and map data from a CMS.</p></div></section><section className="cityChapters">{['HISTORY','CULTURE','ARCHITECTURE','FOOD','PLACES TO VISIT','GALLERY'].map((x,i)=><div key={x}><span>0{i+1}</span><h3>{x}</h3><ArrowUpRight/></div>)}</section><footer><div className="footerLogo">IRAN<span>.</span></div><p>DISCOVER SLOWLY.</p></footer></main>}
